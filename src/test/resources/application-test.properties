# Test Database Configuration
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Disable Flyway in tests (migration scripts use PostgreSQL syntax not supported by H2)
spring.flyway.enabled=false

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.app.usochicamochabackend=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# JWT Configuration for tests
jwt.secret=test-secret-key-for-testing-purposes-only
jwt.secret.password=test-secret-key-for-testing-purposes-only
jwt.issuer.generator=UsochimochaBackend
jwt.expiration=3600000
